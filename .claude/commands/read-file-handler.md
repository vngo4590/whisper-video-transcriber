Read and explain `src/file_handler.py` — the file I/O service.

Steps:
1. Read the file at `src/file_handler.py`.
2. Describe `FileHandler.save_transcription()`: inputs, output file naming convention, return value.
3. Explain why file I/O is isolated here (SRP) instead of living in the controller or transcriber.
4. Suggest how this class could be extended (e.g., saving to a custom path or different format) without changing the controller.
