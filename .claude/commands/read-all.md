Give a full architectural overview of this project.

Steps:
1. Read every source file: `main.py`, `src/models.py`, `src/transcriber.py`, `src/file_handler.py`, `src/media_utils.py`, `src/controller.py`, `src/ui/app.py`, `src/ui/left_panel.py`, `src/ui/right_panel.py`.
2. Produce a module map showing each file, its single responsibility, and which other modules it imports from.
3. Explain which SOLID principles and GRASP patterns apply and where in the code.
4. Describe the full request/response flow from "user clicks Transcribe" to "transcript appears in the text box".
5. Identify the most likely extension points if someone wanted to add a new feature (e.g. different AI backend, export to SRT, drag-and-drop support).
