Read and explain `src/models.py` — the shared configuration constants for this project.

Steps:
1. Read the file at `src/models.py`.
2. List every constant, its type, its current value, and what it controls in the app.
3. Note which other modules import from this file (GRASP Information Expert role).
