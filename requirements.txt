# Core AI / transcription
openai-whisper>=20250625
anthropic>=1.4.0

# Video / audio processing
ffmpeg-python>=0.2.0
opencv-python>=5.0.0.93

# Image processing
Pillow>=12.3.0

# Numerical computing (required by whisper, easyocr, opencv)
numpy>=2.5.2
scipy>=1.18.1

# OCR — on-screen text extraction
easyocr>=1.7.2

# Drag-and-drop file support for tkinter
tkinterdnd2>=0.6.3

# Deep learning backend (required by whisper and easyocr)
# CPU builds come from the PyTorch index; keep the four in step with each other:
#   pip install -U --index-url https://download.pytorch.org/whl/cpu \
#       torch torchvision torchaudio torchcodec
torch>=2.14.0
torchvision>=0.29.0
torchaudio
torchcodec>=0.16.0

# Optional — speaker diarization (requires a Hugging Face token and
# access to the gated pyannote/speaker-diarization-3.1 model)
# pip install pyannote.audio

# Optional — download media from a pasted video URL.
# Without it the app still runs; the URL input is disabled.
# Keep it current: YouTube changes break older releases.
# pip install -U yt-dlp
